1、python socketserver 实现FTP
2、实现功能:
    1.用户认证
    2.上传下载文件
    3.查看文件
    4.切换目录
    5.家目录限制
    6.用户磁盘配额限制
    7.支持多用户同时登录
3、服务端启动：
    Linux下python bin/run.py
4、添加新用户：
    通过服务端下db下acc_add.py 添加
5、客户端使用：
    最好不要在pycharm中使用（无法打印进度）
    上传文件：先切换到要上传的路劲下，put loacalfile 即可
    下载文件：先切换到要上传的路劲下，get filename 即可
    切换目录和查看 按照Linux下执行命令方式即可
6、同一用户可在同时登录多个但是不能同时操作同一个文件
7、不同用户可同时上传下载